import React from "react";
import PropTypes from "prop-types";
import Login from "@/components/Pages/Login";

const page = (props) => {
  return <Login />;
};

page.propTypes = {};

export default page;
